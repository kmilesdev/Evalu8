You are an expert full-stack SaaS engineer.

Build a production-ready SaaS web application called Evalu8.

Evalu8 is an AI-powered hiring platform that simulates real workplace scenarios and evaluates candidate decision-making before the first interview.

🧱 TECH STACK REQUIREMENTS

Use the following stack:

Frontend:

React (Vite)

TailwindCSS

React Router

Backend:

Node.js

Express.js

Database/Auth:

Supabase (Postgres + Auth + Row Level Security)

AI:

OpenAI API

📁 PROJECT STRUCTURE

Create the following folders:

/client
/server
/server/routes
/server/controllers
/server/services
/server/middleware
/server/prompts
/server/db

🌐 APP FLOW

There are two user types:

Recruiter (must sign up / login)

Candidate (no login required)

Recruiter:

Signs up/logs in

Creates a Job Simulation

Receives a shareable link

Reviews candidate reports

Candidate:

Opens share link

Completes intake form

Completes AI simulation interview

Submits responses

📄 FRONTEND PAGES (React)

Recruiter Pages:

/signup

/login

/dashboard

/jobs/new

/jobs/:jobId

/jobs/:jobId/candidates/:appId

Candidate Pages:

/apply/:jobToken

/apply/:jobToken/interview

/apply/:jobToken/complete

🗄 DATABASE TABLES (Supabase SQL)

Create tables:

profiles
jobs
applications
messages
evaluations
flags

jobs table

id uuid primary key

owner_id uuid

title text

description text

simulation_type text default 'problem_solving'

seniority_level text default 'mid'

time_limit_minutes int default 15

num_questions int default 8

scoring_weights jsonb

job_token text unique

is_active boolean

created_at timestamp

applications

id uuid primary key

job_id uuid

status text

candidate_name text

candidate_email text

location text

work_auth text

availability_date date

years_experience numeric

desired_comp text

started_at timestamp

submitted_at timestamp

messages

id uuid primary key

application_id uuid

role text

content text

created_at timestamp

evaluations

id uuid primary key

application_id uuid unique

overall_score int

decision_quality int

communication_clarity int

structured_process int

risk_awareness int

professional_judgment int

recommendation text

summary text

strengths jsonb

concerns jsonb

created_at timestamp

flags

id uuid primary key

application_id uuid

severity text

category text

excerpt text

created_at timestamp

🔐 SUPABASE AUTH

Recruiters must sign up/login using Supabase Auth.

Only allow recruiters to view jobs and applications that they own.

Enable Row Level Security for jobs and applications.

🔗 API ROUTES (Express)

Recruiter:
POST /api/jobs
GET /api/jobs
GET /api/jobs/:jobId
GET /api/jobs/:jobId/applications
GET /api/applications/:appId/report
PATCH /api/applications/:appId/status

Candidate:
GET /api/public/job/:jobToken
POST /api/public/applications
POST /api/public/applications/:appId/message
POST /api/public/applications/:appId/submit

🤖 AI INTERVIEW ENGINE

Create service:

/server/services/interviewAgent.js

The AI should:

Ask ONE question at a time

Adapt follow-up questions

Keep interview professional

Track potential red flags

Use:
simulation_type = "problem_solving"

Default:

8 total questions

15 minute expected time

mid seniority level

Return JSON ONLY:
{
"assistant_message": "",
"detected_flags": [],
"stage": ""
}

🧠 AI EVALUATION ENGINE

Create:

/server/services/evaluationAgent.js

This should evaluate transcript and return:

{
"decision_quality": 0,
"communication_clarity": 0,
"structured_process": 0,
"risk_awareness": 0,
"professional_judgment": 0,
"overall_score": 0,
"recommendation": "strong_yes | yes | maybe | no",
"summary": "",
"strengths": [],
"concerns": []
}

📤 CANDIDATE SUBMIT FLOW

On submit:

Retrieve transcript

Run evaluationAgent

Save evaluation

Save flags if present

Update application status = completed

📊 DASHBOARD

Recruiter dashboard must show:

Jobs created

Number of candidates per job

Status of applications

Clickable candidate report

📦 ENV VARIABLES

SUPABASE_URL
SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
OPENAI_API_KEY

🎨 UI

Create a clean modern SaaS UI using TailwindCSS.

Brand name: Evalu8

Tagline:
"Simulate real work before the first interview."

🚀 FINAL GOAL

Deliver a working MVP where:

Recruiter creates job

Candidate completes simulation

Recruiter views evaluation report